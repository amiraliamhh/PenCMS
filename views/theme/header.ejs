<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0-beta.2/css/bootstrap.min.css" integrity="sha384-PsH8R72JQ3SOdhVi3uxftmaW6Vc51MKb0q5P2rRUpPvrszuE4W1povHYgTpBfshb" crossorigin="anonymous">
    <link rel="stylesheet" href="/stylesheets/style.css">
    <title>Pen CMS</title>
</head>
<body>
    
    <div class="container-fluid header">
        <h1>Welcome to Pen CMS!</h1>
        <h3>Pen CMS is a blogging Content Management System written entirely in Javascript.</h3>
    </div>
    <div class="container-fluid navbar">
        <ul class="nav nav-pills nav-fill">
            <% for (let i = 0; i < menu.length; i++) { %>
                <% if (current == menu[i]) { %>
                    <li class="nav-item">
                        <strong><a href="<%= links[i] %>" class="nav-link" ><%= menu[i] %></a></strong>
                    </li>
                <% } else { %>
                    <li class="nav-item">
                        <a href="<%= links[i] %>" class="nav-link"><%= menu[i] %></a>
                    </li>                    
                <% } %>
            <% } %>            
        </ul>
    </div>
    <br>
    <% if (user) { %>
        <div class="alert alert-primary container" role="alert">
            Hi <%= user.username %>! Welcome to PenCMS!
        </div>
    <% } else { %>
        <div class="alert alert-danger container" role="alert">
            You are not logged in yet!
        </div>
    <% } %>